# <p>Requirements:
python >= 3.10<br><br>
pygame (installable via pip; "pip install pygame")
</p>

# <p>Running the program
navigate to the directory with main.py<br><br>
run main.py in a python interpreter &quot;e.g. python3 ./main.py&quot;<br>
</p>
